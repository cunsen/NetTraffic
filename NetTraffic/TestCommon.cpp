#include "stdafx.h"
#include "Vlpp.h"
using namespace vl;
using namespace unittest;

TEST_CASE(TestDataSize)
{
	vint nsizeData = sizeof(DWORD);
	vint nsizeVint = sizeof(vint);
	vint nsizeVUint = sizeof(vuint);
	vint ulong = sizeof(unsigned long);

}

